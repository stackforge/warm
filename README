Warm - To deploy a simple OpenStack environment from template
=============================================================

  This project doesn't claim to be a solution to replace heat, it just try
  to expose a simple way to deploy small environement from template.
